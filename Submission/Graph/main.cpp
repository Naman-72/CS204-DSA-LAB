#include <iostream>
using namespace std;
#include "graph.h"
#include<cmath>
#include "graphnode.h"

int main()
{/*
    int n;
    int e;
    cin>>n>>e;
    graph* G[n+1];
    for(int i = 1; i<=n;i++)
    {
        G[i] = new graph();
        G[i]->addE(i);
    }
   for(int i = 1; i<=e;i++)
    {
        int a, b;
        cin>>a>>b;
        if(G[i]->)
    }
*/
   int n = 3;
   n = log2(n);
   cout<<n;
    return 0;
}
    /*int A[n+1];
    for(int i = 0; i<= n; i++)
    {
        A[i] = 0;
    }
    int o = 1;
    for(int i =0; i<e;i++)
    {
        int a, b;
        cin>>a>>b;
        A[a] = A[a] +1;
        A[b] = A[b] +1;
    }
    for(int i = 1; i<=n;i++)
    {
        if(A[i]==0)
        {
            o  = 0;
        }
    }
    if(o == 0)
    {
      cout<<"N"<<endl;
      cout<<0<<endl;
      return 0;
    }
*/
